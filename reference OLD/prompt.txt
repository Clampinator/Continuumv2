When inserting a span using the Lifeline Spreadsheet, we need you to use the same insert span function that the lifeline uses. The lifeline's version has special compensations built into it that ensure that the entire timeline is not shifted when a span is inserted. Those compensators are vital. Just use the exact same code. Don't even copy it, Just reference it if you can.
Also, when editing an event in the spreadsheet, make sure that you update not only the location of the node on the lifeline, but also any Experiences associated with it. The Save Changes button in the Spreadsheet should update any associated Experiences start and end dates. In fact, just make sure that pressing Save Changes on an entry in the spreadsheet updates any and all information from the spreadsheet to the lifeline. And make sure to use the Insert compensator if needed.
Add a Map Pin button just to the right of the Location entry field, so that when the user enters a location, we update the map.
Also, intercept the ENTER key when in the Location text entry field, so any valid location entered in there just updates the map.
Finally, can you find out why adding location information into the spreadsheet does not seem to link that event's location to the SpaceTime map, line adding location data to an Event in the lifeline does. Fix that. Let me know if you need to know where the SpaceTime module is for this problem.